const nullVariable = null;

console.log(typeof nullVariable);

//Mesmo sendo null, o retorno será de um objeto, ao tentar verificar o tipo.

console.log(nullVariable);

//No entanto, o valor da variável continua sendo null